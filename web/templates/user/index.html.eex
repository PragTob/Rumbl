<h1>Listing Users</h1>

<table class="table">
  <%= for user <- @users do %>
    <tr>
      <td><strong><%= first_name(user) %></strog></td>
      <td><%= link "view", to: user_path(@conn, :show, user.id) %> </td>
    </tr>
  <% end %>
</table>
